import { createFileRoute } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { PageIntro, WHATSAPP_LINK } from "@/components/site";
import { menuItems } from "@/lib/asali";
export const Route = createFileRoute("/menu")({ head: () => ({ meta: [{ title: "Menu — Asali Pastries" }, { name: "description", content: "Browse selected Asali Pastries menu items and prices in Bahraini dinars." }, { property: "og:title", content: "Asali Pastries Menu" }, { property: "og:description", content: "Browse Turkish pastries, pizza and sides from Asali Pastries." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: MenuPage });
function MenuPage(){return <><PageIntro eyebrow="Our menu" title="Fresh from the oven." copy="A selection transcribed from the current shop menu. Prices are in Bahraini dinars and may change; please confirm when ordering."/><section className="section-pad"><div className="page-shell"><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{menuItems.map(item=><article key={item.name} className="grid grid-cols-[6.5rem_minmax(0,1fr)] overflow-hidden rounded-lg border border-border bg-card"><img src={item.image} alt={item.name} className="h-full min-h-40 w-full object-cover"/><div className="p-5"><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-brand-warm">{item.category}</p><h2 className="mt-2 text-xl font-semibold leading-tight">{item.name}</h2><div className="mt-4 text-sm font-semibold">{item.small&&<p>Small {item.small} BHD</p>}{item.large&&<p>Large {item.large} BHD</p>}{item.price&&<p>{item.price} BHD</p>}</div><Button asChild size="sm" className="mt-4"><a href={WHATSAPP_LINK} target="_blank" rel="noreferrer">Order</a></Button></div></article>)}</div><p className="mt-8 text-sm text-muted-foreground">Please contact the shop to confirm availability, ingredients and the latest prices.</p></div></section></>}